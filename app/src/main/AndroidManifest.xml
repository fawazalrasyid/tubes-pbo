<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".CodeLearn"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/logo_tbs"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_tbs"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning"
        tools:targetApi="m">
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity android:name=".WebviewActivity" />
        <activity android:name=".PustakaActivity" />
        <activity android:name=".ScoreActivity" />
        <activity android:name=".PostTestActivity" />
        <activity android:name=".ModuleActivity" />
        <activity android:name=".CourseDetailActivity" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".StartActivity" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>